NODE_ENV=production
PORT=3000
HOSTNAME=127.0.0.1

# 0 = full CMS with MariaDB  |  1 = static MDX blog, no database
STATIC_SITE=0

# 1 GB server: keep connection_limit low (see deploy/DEPLOY-1GB.md)
# 4 GB Ubuntu: connection_limit=10 is fine (see deploy/DEPLOY-UBUNTU.md)
# Create DB + user: bash scripts/setup-database.sh
DATABASE_URL="mysql://eatherahmed_app:STRONG_PASSWORD_HERE@localhost:3306/eatherahmed?connection_limit=10"

# Generate with: openssl rand -base64 48
JWT_SECRET="q6xE23IsEAzneoT89RZq2f4uoWe7Zw25NV06nVgRWz/bJ1LxG+roctJgJV+8lCOY"

# Used only on first `npm run db:seed` — change password in Admin → Account after login
ADMIN_EMAIL="eather.ahmed@gmail.com"
ADMIN_PASSWORD="P4GdrK1rpnipH8jf"
